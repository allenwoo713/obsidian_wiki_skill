# Firecrawl PDF backend 配置
# 复制本文件为 .env 并填入真实值后启用
# .env 不会被 git 跟踪（已在 .gitignore 中忽略）

# Firecrawl API key（从 https://firecrawl.dev 获取）
FIRECRAWL_API_KEY=

# 启用 Firecrawl backend（设为 firecrawl 启用，留空或删除走本地 PdfParser）
PDF_PARSER_BACKEND=firecrawl
