[pytest]
# Only the repository's canonical test suite participates in pytest discovery.
# Local evaluation/review worktrees and deployment caches can contain their own
# test packages, but they are ignored deployment artifacts, not this project's
# tests.
testpaths = tests
norecursedirs = tmp .cache .tools .venv .pytest_tmp pytest-cache-files-*
